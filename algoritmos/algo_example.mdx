---
title: Ejemplo de algoritmo
description: Ejemplo (placeholder) de documentación de un algoritmo C2D en Empower‑X.
sidebar_position: 2
---

# Ejemplo de algoritmo (placeholder)

Esta página es un **ejemplo** de cómo documentaremos un algoritmo Compute-to-Data en Empower‑X.

## Resumen

- **Nombre**: `algo_example`
- **Objetivo**: ilustrar la estructura mínima de la documentación
- **Entradas**: dataset + parámetros (a definir)
- **Salidas**: métricas agregadas/anónimas (a definir)

## Interfaz (borrador)

| Campo | Tipo | Descripción |
|------|------|-------------|
| `input` | `dataset` | Referencia al dataset dentro del contenedor seguro |
| `params` | `object` | Parámetros de ejecución (p.ej. umbrales, filtros) |
| `result` | `object` | Resultados agregados listos para exportación |

## Privacidad

Este algoritmo se ejecuta **donde residen los datos** (C2D). El objetivo es evitar que los datos en bruto salgan del entorno protegido.

## Próximos pasos

- Reemplazar esta página con un algoritmo real y su especificación
- Añadir ejemplos de ejecución y validación
